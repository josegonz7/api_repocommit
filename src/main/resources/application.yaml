spring:
  application:
    name: api_repo_commit
  datasource:
    url: jdbc:mysql://localhost:3306/repos_commit
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true